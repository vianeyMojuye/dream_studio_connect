// useUserPreferences — stub (complété Story 1.5)
// Gère theme (dark/light) et langue (fr/en) via localStorage
// export function useUserPreferences() {
//   return { theme, setTheme, language, setLanguage }
// }
export {}
