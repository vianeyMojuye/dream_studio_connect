// Webhook WhatsApp Business API — stub (complété Story 3.5)
// Vérification HMAC obligatoire avant tout traitement
export async function POST() {
  return new Response('WhatsApp webhook — à configurer (Story 3.5)', { status: 501 })
}
export async function GET() {
  return new Response('WhatsApp webhook verification — à configurer (Story 3.5)', { status: 501 })
}
