// Webhook Orange Money / MTN MoMo — stub (complété Story 5.1)
// Vérification signature HMAC obligatoire avant activation abonnement
export async function POST() {
  return new Response('MoMo webhook — à configurer (Story 5.1)', { status: 501 })
}
